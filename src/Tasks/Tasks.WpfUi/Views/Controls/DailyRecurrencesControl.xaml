<UserControl 
    x:Class="Tasks.WpfUi.Views.Controls.DailyRecurrencesControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:Tasks.WpfUi.Views.Controls"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d" 
    d:DesignWidth="800"
    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
    d:DataContext="{d:DesignInstance local:DailyRecurrencesControl, IsDesignTimeCreatable=False}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>


        <TextBlock 
            Grid.Row="0"
            Text="{Binding ViewModel.Date.Date, Mode=OneWay}" 
            d:Text="06/05/2022"
            FontSize="16" Margin="0,20,0,20" />

        <ListView 
            Grid.Row="1"
            ItemsSource="{Binding ViewModel.Recurrences, Mode=OneWay}" 
            d:ItemsSource="{d:SampleData ItemCount=5}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ui:Card >
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding Completed}" Width="48" MinWidth="0"/>
                            <TextBlock FontSize="20" Text="{Binding Path=Event.Name}"  />
                        </StackPanel>
                    </ui:Card>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>


    </Grid>
</UserControl>
